name: Project Health Check

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Basic Check
      run: |
        echo '=== Repository Status ==='
        git status
        echo '=== File Count ==='
        find . -type f | wc -l
